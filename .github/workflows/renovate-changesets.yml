name: Add changeset to Renovate updates

on:
  pull_request_target:
    types: [opened, synchronize, labeled]

jobs:
  update:
    uses: mscharley/.github/.github/workflows/renovate-changesets.yml@de06745e8298d9a77c20fd040c8fde630d31eb5e # main
    secrets:
      token: ${{ secrets.DEPENDENCY_UPDATE_GITHUB_TOKEN }}
