name: Add changeset to Renovate updates

on:
  pull_request_target:
    types: [opened, synchronize, labeled]

jobs:
  update:
    uses: mscharley/.github/.github/workflows/renovate-changesets.yml@3ea5efb827af3ad6391fa1a055197cea50931c25 # main
    secrets:
      token: ${{ secrets.DEPENDENCY_UPDATE_GITHUB_TOKEN }}
