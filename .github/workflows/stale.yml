name: 'Close stale issues and PR'
on:
  schedule:
    - cron: '30 1 * * *'
  workflow_dispatch:

jobs:
  stale:
    uses: mscharley/.github/.github/workflows/stale.yml@99c53eeb2e7be913acc1d75fc8042417405380f5 # main
    permissions:
      issues: write
      pull-requests: write
      discussions: write
