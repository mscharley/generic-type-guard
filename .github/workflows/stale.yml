name: 'Close stale issues and PR'
on:
  schedule:
    - cron: '30 1 * * *'
  workflow_dispatch:

jobs:
  stale:
    uses: mscharley/.github/.github/workflows/stale.yml@de06745e8298d9a77c20fd040c8fde630d31eb5e # main
    permissions:
      issues: write
      pull-requests: write
      discussions: write
