{
  "name": "generic-type-guard-meta",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "test-watch": "lerna run --parallel test-watch",
    "stryker": "lerna run stryker",
    "codecov-report": "lerna run --parallel codecov-report"
  },
  "devDependencies": {
    "lerna": "3.20.2"
  }
}
