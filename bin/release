#!/usr/bin/env bash

${1:?You must specify at least one parameter to this script.};
set -e
set -x
git checkout master
git pull --ff-only
VERSION=$(npm version "$@")
git push origin master tag "$VERSION"
npm run compile
npm publish
